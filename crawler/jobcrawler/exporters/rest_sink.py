import httpx, os\n\nBACKEND = os.getenv(\"BACKEND_URL\", \"http://backend:8080\")\n\nclass RestSink:\n    def process_item(self, item, spider):\n        # Send to Celery via REST task (or direct broker)\n        httpx.post(f\"{BACKEND}/healthz\")  # warmup\n        # In real: a /ingest endpoint or direct Redis enqueue. Here we assume a task endpoint exists later.\n        return item\n